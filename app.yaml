name: dyau-ai

static_sites:
  - name: web
    github:
      repo: Shivammishra0001/DYAU-AI
      branch: main
    source_dir: /
    build_command: npm run build
    output_dir: dist
    index_document: index.html
    error_document: index.html
